function randomiseSearch(){
    // This function is called when the user clicks the "I'm Feeling Lucky" button
    // It will randomly select a location from the list of locations and redirect the user to that location
    // This is done by setting the location to a random index in the list of locations
    // The user is then redirected to the map page with the new location
    // This is done by using the useHistory hook from react-router-dom
    
}
export default randomiseSearch;